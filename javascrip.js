/**$.getJSON("dat.json", function(json) {
    var a=json.subsContent; // this will show the info it in firebug console
	var b=json.subsTime;
	var c=json.summarySentences;
	var d=json.summaryTime;
	document.getElementById("test").innerHTML =a[0];
});
*/
